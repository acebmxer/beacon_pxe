FROM alpine:3.20
RUN apk add --no-cache dnsmasq
# Config is bind-mounted at /dnsmasq/dnsmasq.conf (generated by the web app).
ENTRYPOINT ["dnsmasq", "--keep-in-foreground", "--log-facility=-", \
            "--conf-file=/dnsmasq/dnsmasq.conf"]
