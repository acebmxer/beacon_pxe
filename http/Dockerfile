# nginx serving the HTTP boot root to iPXE clients, with the Beacon config baked
# in so end users need only the compose file (no repo checkout).
FROM nginx:alpine
COPY nginx.conf /etc/nginx/conf.d/default.conf
