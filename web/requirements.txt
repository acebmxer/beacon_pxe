# fastapi 0.139.x pulls starlette >=1.3.1, which carries the fixes for the
# unauthenticated multipart / FileResponse-Range DoS advisories (and the
# request.url host-header issues). Bumping fastapi is the only way to reach a
# patched starlette, since fastapi pins the starlette range.
fastapi==0.141.1
uvicorn[standard]==0.52.4
# 3.1.6 fixes the sandbox |attr RCE advisory (PYSEC-2026-1471).
jinja2==3.1.6
sqlalchemy==2.0.51
passlib[bcrypt]==1.7.4
# Pinned to 4.0.x: passlib 1.7.4 can't read the version of bcrypt >=4.1 (noisy
# but harmless warning), and bcrypt >=5 is fatal — its hashpw() raises on
# secrets over 72 bytes, which passlib's load-time self-test triggers, so the
# app dies on startup. Dependabot is told to ignore bcrypt >=4.1 accordingly.
bcrypt==4.0.1
# 0.0.32 fixes the multipart/urlencoded parser DoS and path-traversal advisories.
python-multipart==0.0.32
itsdangerous==2.2.0
